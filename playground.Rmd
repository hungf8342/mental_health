---
title: "Playground"
author: "Frances Hung"
date: "10/21/2017"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,warning=FALSE)
```

## Playground


```{r}
require(gtrendsR)
require(ggplot2)
require(dplyr)
```

## Making Dataframes

This gives us a master dataframe of search frequencies of “depression” over the past 12 months in the US which relate for sure to mental health. We can take different dataframes using "$": see the dataframe for details.

```{r}
trend<-gtrends("depression",c("US"),time="today 12-m",category=437)
trend
```

For example, this gives us search frequencies by cities in the U.S.

```{r}
cities_dep<-gtrends("depression",c("US"),time="today 12-m",category=437)$interest_by_city
cities_dep
```

This plots cities_dep.

```{r}
ggplot(cities_dep,aes(x=reorder(location,hits),y=hits))+geom_bar(stat="identity")+theme(axis.text.x = element_text(angle = 90, hjust = 1))
```

